# Installing Starship via Ansible

Small Ansible role to handle installing the [Starship](https://starship.rs/) shell prompt on Linux.

You can set the `starship_version` variable to target a specific release.
